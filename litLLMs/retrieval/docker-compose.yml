version: '3.8'

services:
  app:
    build: .
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
    restart: unless-stopped
    environment:
      - PYTHONPATH=/app:/app/src

